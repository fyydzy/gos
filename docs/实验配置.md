## ALFWorld 实验配置和参数及在原文中的具体位置：

### 1. 数据集划分与评估指标 (Section 4.1, Page 6)

- **数据集规模**：实验在完整的 140 个 episode（回合）划分上进行评估 。  

- **指标定义**：主要评估指标为任务的平均奖励（Average reward） 。由于 ALFWorld 中的奖励是二元的，因此平均奖励等同于成功率（Success rate） 。此外，还记录了平均总 token 消耗量和仅智能体运行时间（agent-only runtime），runtime 不包含环境初始化时间 。  

- **重复实验**：每种模型与方法的组合均运行两次，最终报告两次运行的平均值 。  

### 2. 对比基线 (Baselines) 配置 (Section 4.1, Page 6)

- **Vanilla Skills (原始技能基线)**：在 ALFWorld 上，Vanilla Skills 采用了官方 Agent Skills 的格式和参考库设置 。  

- **Vector Skills (向量检索基线)**：使用 `openai/text-embedding-3-large` 嵌入模型进行语义检索，维度为 3072 维 。  

### 3. Graph of Skills (GoS) 配置 (Section 4.1, Page 6 & Appendix B, Page 15)

- **嵌入模型**：与 Vector Skills 一致，使用 `openai/text-embedding-3-large` 。  

- **查询重写模块**：在实验中，关闭了可选的查询重写（query-rewrite）模块，直接使用原始的任务指令作为检索查询词 。  

- **逆向图扩散权重 (Appendix B, Table 6, Page 15)**：在关系图检索的配置中，各类边缘类型向后传播的权重设定为：依赖关系（Dependency）设为 1.0，工作流（Workflow）设为 0.5，语义（Semantic）设为 0.2，替代方案（Alternative）设为 0.1 。  

### 4. 评估的大语言模型 (Section 4.1, Page 6)

- 实验中统一使用了三个模型系列：**Claude Sonnet 4.5**、**MiniMax M2.7** 和 **GPT-5.2 Codex** 。  

### 5. 重试策略与异常处理 (Section 4.1, Page 6)

实验采用了统一的重试策略来处理异常情况 ：  

- **环境构建失败**：如果环境构建失败，系统会重建并重新运行任务，最多额外尝试两次 。如果重试后仍然失败，这些任务将被视为未解决的基础设施故障而排除，不计入模型失败 。  
- **智能体超时 (Timeouts)**：

  - 如果智能体已经执行了很长时间后超时，记为发生实质性的执行失败，奖励记为 0，并保留在总计结果中 。  
  - 如果超时发生在建立有意义的运行之前（即启动失败），则重新运行该试验 。  
- 这套重试协议同样应用于完整的 140 个 ALFWorld 评估集中 。  

## 实验配置表



| 配置项        | 论文主实验                                                   | 复现                                                         |
| ------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| Benchmark     | ALFWorld                                                     | ALFWorld                                                     |
| 任务 split    | full 140 episodes；推测对应 `valid_seen / eval_in_distribution` | `--split dev` alfworld_run.py 默认值，与论文的140也是对应的  |
| 模型          | Claude Sonnet 4.5、MiniMax M2.7、GPT-5.2 Codex               | `gpt-4o-mini`                                                |
| 对比方法      | Vanilla Skills、Vector Skills、GoS                           | `all_full`、`vector`、`gos`，可加 `none`                     |
| 技能库        | 技能库规模论文主表未在 ALFWorld 段单独展开；代码默认 `skills_200` | `data/skillsets/skills_200`alfworld_run.py 默认值            |
| Workspace     | 预构建 GoS workspace                                         | `data/gos_workspace/skills_200_v1`alfworld_run.py 默认值     |
| Embedding     | `openai/text-embedding-3-large`, 3072 dim                    | 同论文                                                       |
| Query rewrite | 论文说禁用 optional query rewrite                            | `evaluation/skill.py` 里初始化 `SkillGraphRAG.Config设置` `enable_query_rewrite=False` |
| Max steps     | 代码默认 30                                                  | `--max_steps 30`，alfworld_run.py 默认值                     |
| Runs          | 每个 model-method setting 跑 2 次取均值                      | 需要用不同 `exp_name` 手动跑两轮                             |
| 指标          | average reward、tokens、agent-only runtime                   | 同论文                                                       |

### 关于技能库

- `skills_200`：无 ALFWorld 专用技能（200 个通用技能）
- `skills_500` / `skills_1000` / `skills_2000`：在对应规模技能库之上均含 **37 个** `alfworld-*` 专用技能（后两者在 500 基础上继续扩充其它领域技能，ALFWorld 部分相同）

路径：`data/skillsets/skills_500/`；GoS 工作区：`data/gos_workspace/skills_500_v1/`。

## skills_500 中的 ALFWorld 专用技能（37 个）

来源：`data/skillsets/skills_500/alfworld-*/SKILL.md`。仅存在于 `skills_500` 及以上规模库，`skills_200` 中没有。

| 技能名 | 用途简述 |
| --- | --- |
| alfworld-appliance-navigator | 导航到加热/冷却/清洁所需的家电（微波炉、炉灶、冰箱、水槽等） |
| alfworld-appliance-preparer | 在使用家电前确保其开/关状态正确（如先打开微波炉再放入物品） |
| alfworld-clean-object | 用 sinkbasin 等清洁容器清洗物体（如 clean potato） |
| alfworld-device-operator | 操作设备（台灯、微波炉、冰箱）与目标物体交互（如台灯下 examine、加热） |
| alfworld-environment-scanner | 进入环境后扫描可见物体与容器，建立初始空间认知 |
| alfworld-goal-interpreter | 解析自然语言任务目标，拆成可执行子目标与所需物体 |
| alfworld-heat-object-with-appliance | 用微波炉/炉灶/烤箱加热物体 |
| alfworld-inventory-management | 收集并跟踪多个同类物体（如 put two cellphone in bed） |
| alfworld-locate-target-object | 导航到疑似位置并确认目标物体是否存在 |
| alfworld-location-navigator | 移动到指定容器或物体位置（go to） |
| alfworld-navigation-planner | 规划在容器间移动的路径，用于搜索目标物体 |
| alfworld-object-cooler | 用冰箱等冷却已持有的物体 |
| alfworld-object-disposer | 将物体放入 garbagecan 等处置容器 |
| alfworld-object-heater | 持有物体后导航至加热设备并完成加热 |
| alfworld-object-locator | 根据观测与常识推理，排序可能藏有目标物体的容器并导航 |
| alfworld-object-picker | 从指定容器 take 起目标物体 |
| alfworld-object-placer | 将手中物体 put/move 到目标容器 |
| alfworld-object-retriever | 在确认物体可见后从容器取出 |
| alfworld-object-state-inspector | 前往容器检查内部/表面有什么物品 |
| alfworld-object-state-modifier | 用设备改变物体状态（加热、冷却、清洁） |
| alfworld-object-storer | 将手中物体存入目标容器（含需先 open 的抽屉/柜子） |
| alfworld-object-transporter | 从源容器取出物体并运送到目标容器 |
| alfworld-open-receptacle | 打开 closed 容器以查看或操作内部 |
| alfworld-receptacle-closer | 搜索完毕后关闭已打开的容器 |
| alfworld-receptacle-finder | 寻找合适的空位或容器来放置物体 |
| alfworld-receptacle-navigator | 规划并执行 go to 目标容器 |
| alfworld-receptacle-opener | 对 closed 容器执行 open |
| alfworld-receptacle-operator | 根据任务需要 open 或 close 容器 |
| alfworld-receptacle-preparer | 放置物体前检查目标容器是否可用（如需先打开） |
| alfworld-receptacle-searcher | 在环境中系统搜索特定容器（如 garbagecan） |
| alfworld-search-pattern-executor | 按常识顺序依次检查多个可能位置以找目标物体 |
| alfworld-search-verifier | 初次搜索失败后复查已访问位置，避免漏检 |
| alfworld-storage-explorer | 系统探索抽屉/柜子/架子，寻找合适的存放位置 |
| alfworld-task-verifier | 子动作完成后判断任务是否已达成或动作是否无效 |
| alfworld-temperature-regulator | 通过冰箱/微波炉改变物体温度并放到目标位置 |
| alfworld-tool-locator | 搜索 desklamp、knife 等工具（检查桌面、架子等） |
| alfworld-tool-user | 对目标物体执行 clean / heat / cool / use 等工具交互 |

### 按任务类型的大致分组

| 类型 | 相关技能 |
| --- | --- |
| 理解与规划 | goal-interpreter、environment-scanner、navigation-planner、task-verifier |
| 移动与搜索 | location-navigator、receptacle-navigator、object-locator、search-pattern-executor、search-verifier、receptacle-searcher、storage-explorer |
| 开关容器 | open-receptacle、receptacle-opener、receptacle-closer、receptacle-operator、receptacle-preparer |
| 拿放物体 | object-picker、object-retriever、object-placer、object-storer、object-transporter、object-disposer、inventory-management |
| 改变物体状态 | clean-object、object-heater、heat-object-with-appliance、object-cooler、object-state-modifier、temperature-regulator |
| 设备与工具 | appliance-navigator、appliance-preparer、tool-locator、tool-user、device-operator |
| 检查状态 | object-state-inspector、locate-target-object、receptacle-finder |
